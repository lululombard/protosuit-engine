---
all:
  children:
    hub:
      hosts:
        hub_pi:
          ansible_host: "127.0.0.1" # Replace with actual WiFi IP in host_vars
    pizeros:
      hosts:
        left_pizero:
          ansible_host: "192.168.42.1" # Replace with actual WiFi IP in host_vars
        right_pizero:
          ansible_host: "192.168.43.1" # Replace with actual WiFi IP in host_vars
