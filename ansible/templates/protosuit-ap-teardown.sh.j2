#!/bin/bash
# AP interface teardown - runs as ExecStopPost for hostapd
# Deployed by Ansible - do not edit manually

source /etc/protosuit/ap.env

# Flush IP address
ip addr flush dev "$AP_INTERFACE" || true

# Return interface to NetworkManager control
nmcli device set "$AP_INTERFACE" managed yes || true
